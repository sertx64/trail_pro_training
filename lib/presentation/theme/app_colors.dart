import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromRGBO(1, 57, 104, 1);
  static const primaryLight = Color.fromARGB(200, 1, 57, 104);
  static const accent = Color.fromRGBO(255, 132, 26, 1);
  static const background = Colors.white;
  static const text = Colors.black;
  static const textLight = Colors.white;
  static const success = Colors.green;
  static const error = Colors.red;
  static const grey = Colors.grey;
  
  // Состояния
  static const disabledGrey = Color.fromARGB(255, 128, 128, 128);
  static const completedGreen = Color.fromARGB(255, 200, 255, 200);
  static const pastGrey = Color.fromARGB(255, 224, 224, 224);
} 